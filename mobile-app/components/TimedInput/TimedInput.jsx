// question: string
// answers: string[]
// duration: number
import React from 'react';
import { AppRegistry, Text, TextInput, View } from 'react-native';

export function TimedInput({ question, answers, duration }) {
  return <div>TimedForm</div>;
}
